# Built from the WireView install by ../build.py -- not committed.
#
# These are extracted Porsche wiring documentation (navigation tree and pin
# tables for every model). The tooling is ours and belongs in the repo; the
# data is theirs and belongs on your disk. Rebuild with:
#
#     python ../build.py --out .
#
# The handful of pinouts this project actually depends on are transcribed,
# with commentary, in research/CAYENNE_958_PINOUTS.md.
*.json
pins/
